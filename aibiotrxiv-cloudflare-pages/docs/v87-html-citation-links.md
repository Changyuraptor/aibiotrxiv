# v87 HTML citation links

Public manuscript HTML pages now run a post-render citation linker over the article body. Citation patterns such as `[1]` and `[3, 4]` are converted into clickable links on the dynamic manuscript page, and reference entries at the beginning of reference paragraphs receive matching anchors. PDF citation behavior from v86 is unchanged.
