# Recover/delete state check

Delete actions move records to a deleted state and preserve the record in D1. Permanent purge is a request state, not an immediate destructive deletion from the browser.

Recover actions update the D1 record back to the previous workflow state. Published, accepted, rejected, and unpublished states remain traceable through D1 audit events.
