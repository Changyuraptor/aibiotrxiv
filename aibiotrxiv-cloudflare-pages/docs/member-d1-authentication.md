# Member D1 authentication

Member registration, login, email verification, suspension state, and comment restriction state are stored in Cloudflare D1. Member login uses an HTTP-only cookie session generated by Pages Functions.

The browser does not store membership records as persistent account data.
