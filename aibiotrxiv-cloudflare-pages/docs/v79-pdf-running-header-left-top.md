# v79 PDF running header left-top fix

- Keeps PDF preview and published PDF on the same Cloudflare Browser Rendering template.
- Moves the running header to the upper-left page margin for pages after page 1.
- Keeps page 1 without the running header.
- Keeps bottom-centered page numbers as `Page X`.
