function currentMember(){return window.AIBIO?.memberCurrent?.()||null;}
async function findDraft(id){return window.AIBIO?.memberGetPaper?await window.AIBIO.memberGetPaper(id):null;}
async function upsertMemberPaper(p,action='save'){return await window.AIBIO.memberUpsertPaper(p,action);}
function areaOptions(){return window.AIBIO.researchAreas.map(a=>`<option value="${a.name}">${a.name}</option>`).join('');}
let resultCount=0;
function escapeHtml(str=''){return String(str||'').replace(/[&<>"']/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[s]));}
function sanitizeRich(html=''){const t=document.createElement('template');t.innerHTML=html;const allowed=new Set(['B','STRONG','I','EM','SUP','SUB','BR','P','DIV','SPAN','UL','OL','LI']);const walk=n=>{[...n.childNodes].forEach(c=>{if(c.nodeType===Node.ELEMENT_NODE){if(!allowed.has(c.tagName)){const f=document.createDocumentFragment();while(c.firstChild)f.appendChild(c.firstChild);c.replaceWith(f);walk(n);}else{[...c.attributes].forEach(a=>c.removeAttribute(a.name));walk(c);}}});};walk(t.content);return t.innerHTML.trim();}
function richToPlain(html=''){const d=document.createElement('div');d.innerHTML=html;return d.textContent.trim();}
function richTextToDisplay(html=''){return sanitizeRich(html)||'<span class="muted">No text entered.</span>';}
function makeToolbar(){return `<div class="rich-toolbar"><button type="button" data-command="bold"><strong>B</strong></button><button type="button" data-command="italic"><em>I</em></button></div>`;}
function bindToolbar(editor){editor.querySelectorAll('.rich-toolbar button').forEach(btn=>btn.addEventListener('click',()=>{const target=editor.querySelector('.section-rich-text');target.focus();document.execCommand(btn.dataset.command,false,null);}));}
function makeEditor({id,heading,allowFigure=false,html='',legend='',image=''}){const div=document.createElement('article');div.className='paper-section-editor editing';div.dataset.section=id;div.dataset.heading=heading;div.dataset.allowFigure=allowFigure?'true':'false';div.dataset.saved='false';div.dataset.image=image||'';div.innerHTML=`<div class="section-input-pane"><div class="section-label">${heading}</div><label>Text</label>${makeToolbar()}<div class="section-rich-text" contenteditable="true" data-placeholder="Paste or write the ${heading} text here.">${sanitizeRich(html)}</div><button type="button" class="btn btn-ghost section-ok">OK</button></div><div class="section-figure-pane ${allowFigure?'':'hidden-pane'}"><label>Figure image <span class="muted">JPG or PNG only</span></label><input class="section-image" type="file" accept="image/jpeg,image/png"><div class="image-preview">${image?`<img src="${image}" alt="Uploaded figure preview">`:'No image uploaded'}</div><label>Legend</label><textarea class="section-legend" placeholder="Figure legend for this section.">${escapeHtml(legend)}</textarea></div><div class="section-display" hidden><div class="section-display-text"></div><div class="section-display-figure"></div></div>`;div.querySelector('.section-ok').addEventListener('click',()=>saveSection(div));bindToolbar(div);const rich=div.querySelector('.section-rich-text');rich.addEventListener('paste',()=>setTimeout(()=>{rich.innerHTML=sanitizeRich(rich.innerHTML);},0));const imgInput=div.querySelector('.section-image');if(imgInput)imgInput.addEventListener('change',()=>previewImage(imgInput));return div;}
function addAuthorRow(name='',email=''){const sheet=document.getElementById('authorSheet');if(!sheet)return;const row=document.createElement('div');row.className='author-row';row.innerHTML=`<input class="author-name" placeholder="Author name" value="${escapeHtml(name)}"><input class="author-email" type="email" placeholder="email@example.org" value="${escapeHtml(email)}"><button type="button" class="author-remove" aria-label="Remove author">×</button>`;row.querySelector('.author-remove').addEventListener('click',()=>{if(document.querySelectorAll('.author-row:not(.author-row-head)').length>1)row.remove();});sheet.appendChild(row);}
function collectAuthors(){return [...document.querySelectorAll('.author-row:not(.author-row-head)')].map(row=>({name:row.querySelector('.author-name')?.value.trim()||'',email:row.querySelector('.author-email')?.value.trim()||''})).filter(a=>a.name||a.email);}
function addResultsSection(scroll=true){resultCount++;const wrap=document.getElementById('resultsSections');const div=makeEditor({id:'results-'+resultCount,heading:'Results',allowFigure:true});wrap.appendChild(div);if(scroll)div.scrollIntoView({behavior:'smooth',block:'center'});}
function compressImageFile(file,maxWidth=1200,quality=0.72){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=()=>reject(new Error('Could not read image file.'));reader.onload=()=>{const img=new Image();img.onerror=()=>reject(new Error('Could not load image file.'));img.onload=()=>{const scale=Math.min(1,maxWidth/img.width);const canvas=document.createElement('canvas');canvas.width=Math.round(img.width*scale);canvas.height=Math.round(img.height*scale);canvas.getContext('2d').drawImage(img,0,0,canvas.width,canvas.height);const type=file.type==='image/png'&&file.size<600000?'image/png':'image/jpeg';resolve(canvas.toDataURL(type,type==='image/jpeg'?quality:undefined));};img.src=reader.result;};reader.readAsDataURL(file);});}
async function previewImage(input){const file=input.files[0];const editor=input.closest('.paper-section-editor');const box=editor.querySelector('.image-preview');if(!file){box.textContent='No image uploaded';editor.dataset.image='';return;}if(!['image/jpeg','image/png'].includes(file.type)){alert('Only JPG and PNG files are allowed.');input.value='';return;}box.textContent='Processing image...';try{const dataUrl=await compressImageFile(file);editor.dataset.image=dataUrl;box.innerHTML=`<img src="${dataUrl}" alt="Uploaded figure preview">`;}catch(err){alert('The image could not be processed.');input.value='';box.textContent='No image uploaded';editor.dataset.image='';}}
function saveSection(editor){const html=sanitizeRich(editor.querySelector('.section-rich-text').innerHTML);const legend=editor.querySelector('.section-legend')?.value.trim()||'';const image=editor.dataset.image||'';const allowFigure=editor.dataset.allowFigure==='true';const display=editor.querySelector('.section-display');const resultEditors=[...document.querySelectorAll('[data-section^="results-"]')];const showHeading=!(String(editor.dataset.heading).toLowerCase()==='results'&&resultEditors.indexOf(editor)>0);display.querySelector('.section-display-text').innerHTML=`${showHeading?`<h3>${escapeHtml(editor.dataset.heading)}</h3>`:''}<div class="rich-display-text">${richTextToDisplay(html)}</div><button type="button" class="btn btn-ghost section-edit">Edit</button>`;display.querySelector('.section-display-figure').innerHTML=allowFigure&&image?`<figure><img src="${image}" alt="Submitted figure"><figcaption>${escapeHtml(legend).replace(/\n/g,'<br>')||'No legend entered.'}</figcaption></figure>`:(allowFigure?'<p class="muted">No figure uploaded.</p>':'');editor.querySelector('.section-input-pane').hidden=true;const figPane=editor.querySelector('.section-figure-pane');if(figPane)figPane.hidden=true;display.hidden=false;editor.dataset.saved='true';editor.classList.add('section-saved');editor.classList.remove('editing');display.querySelector('.section-edit').addEventListener('click',()=>editSection(editor));}
function editSection(editor){editor.querySelector('.section-input-pane').hidden=false;const figPane=editor.querySelector('.section-figure-pane');if(figPane&&!figPane.classList.contains('hidden-pane'))figPane.hidden=false;editor.querySelector('.section-display').hidden=true;editor.dataset.saved='false';editor.classList.remove('section-saved');editor.classList.add('editing');}
function sectionData(editor,order){const html=sanitizeRich(editor.querySelector('.section-rich-text').innerHTML);return{order,heading:editor.dataset.heading,text:html,plainText:richToPlain(html),legend:editor.querySelector('.section-legend')?.value||'',image:editor.dataset.image||''};}
function collectSubmission(previous){const editors=[...document.querySelectorAll('.paper-section-editor')];const sections=editors.map((el,i)=>sectionData(el,i+1));const authorList=collectAuthors();const member=currentMember();const draftId=new URLSearchParams(location.search).get('draft');return{...(previous||{}),title:document.getElementById('title').value.trim(),abstract:document.getElementById('abstract').value.trim(),submissionCategory:document.getElementById('submissionCategory')?.value||'AI Research',topic:document.getElementById('topic').value,type:document.getElementById('manuscriptType').value,authors:authorList.map(a=>a.name).filter(Boolean).join(', '),email:authorList[0]?.email||'',authorList,affiliation:document.getElementById('affiliation').value.trim(),aiUse:document.getElementById('aiUse').value.trim(),license:document.querySelector('input[name="license"]:checked')?.value,sections,createdAt:(previous&&previous.createdAt)||new Date().toISOString(),updatedAt:new Date().toISOString(),status:(previous&&previous.isRevisionDraft)?'revision_draft':'draft',paymentStatus:(previous&&previous.paymentStatus&&previous.paymentStatus!=='paid'&&previous.paymentStatus!=='waived')?previous.paymentStatus:'unpaid',reviewStatus:(previous&&previous.isRevisionDraft)?'revision_draft':'draft',memberEmail:member?.email||'',id:draftId||(previous&&previous.id)||'SUB-'+Date.now()};}
function validateSubmission(){if(!document.getElementById('title').value.trim())return alert('Please enter a manuscript title.'),false;if(!document.getElementById('abstract').value.trim())return alert('Please enter an abstract.'),false;const authors=collectAuthors();if(!authors.length||!authors[0].name||!authors[0].email)return alert('Please enter at least one author name and email.'),false;if(!document.getElementById('rightsConfirm').checked)return alert('Please confirm the rights and permissions statement.'),false;if(!document.getElementById('creditConfirm')?.checked)return alert('Please confirm the creator credit statement.'),false;if(!document.getElementById('timestampConfirm')?.checked)return alert('Please confirm the timestamp and record-protection statement.'),false;return true;}
function loadDraftIntoForm(draft){if(!draft)return;document.getElementById('title').value=draft.title||'';document.getElementById('abstract').value=draft.abstract||'';const cat=document.getElementById('submissionCategory');if(cat)cat.value=draft.submissionCategory||'AI Research';document.getElementById('topic').value=draft.topic||document.getElementById('topic').value;document.getElementById('manuscriptType').value=draft.type||document.getElementById('manuscriptType').value;document.getElementById('affiliation').value=draft.affiliation||'';document.getElementById('aiUse').value=draft.aiUse||'';const sheet=document.getElementById('authorSheet');if(sheet){[...sheet.querySelectorAll('.author-row:not(.author-row-head)')].forEach(r=>r.remove());(draft.authorList?.length?draft.authorList:[{name:'',email:''}]).forEach(a=>addAuthorRow(a.name,a.email));}if(draft.license){const radio=document.querySelector(`input[name="license"][value="${draft.license}"]`);if(radio)radio.checked=true;}const groups={};(draft.sections||[]).forEach(sec=>{const key=String(sec.heading||'').toLowerCase();(groups[key]=groups[key]||[]).push(sec);});function setEditor(containerId,sec){const ed=document.querySelector(`#${containerId} .paper-section-editor`);if(!ed||!sec)return;ed.querySelector('.section-rich-text').innerHTML=sanitizeRich(sec.text||'');const lg=ed.querySelector('.section-legend');if(lg)lg.value=sec.legend||'';ed.dataset.image=sec.image||'';const prev=ed.querySelector('.image-preview');if(prev&&sec.image)prev.innerHTML=`<img src="${sec.image}" alt="Uploaded figure preview">`;}setEditor('introductionSection',(groups['introduction']||[])[0]);setEditor('discussionSection',(groups['discussion']||[])[0]);setEditor('methodsSection',(groups['materials and methods']||[])[0]);setEditor('referencesSection',(groups['references']||[])[0]);const results=groups['results']||[];const resultBox=document.getElementById('resultsSections');if(resultBox&&results.length){resultBox.innerHTML='';results.forEach((sec,i)=>{const ed=makeEditor({id:'results-'+i,heading:'Results',allowFigure:true,html:sec.text||'',legend:sec.legend||'',image:sec.image||''});resultBox.appendChild(ed);});}}
async function bindSubmit(){const topic=document.getElementById('topic');if(topic)topic.innerHTML=areaOptions();const form=document.getElementById('submissionForm');if(!form)return;await window.AIBIO?.memberRefresh?.();const member=currentMember();const notice=document.getElementById('submitAuthNotice');if(!member){if(notice){notice.style.display='block';notice.innerHTML='<h2>Member registration required</h2><p>You must register or log in before drafting and submitting a manuscript.</p><div class="hero-actions"><a class="btn btn-cyan" href="/member/register/">Register</a><a class="btn btn-ghost" href="/member/login/">Login</a></div>';}form.style.display='none';return;}if(window.AIBIO?.memberIsSuspended?.(member)){if(notice){notice.style.display='block';notice.innerHTML='<h2>Account suspended</h2><p>'+window.AIBIO.memberSuspensionMessage(member)+'</p>';}form.style.display='none';return;}if(!member.emailVerified){if(notice){notice.style.display='block';notice.innerHTML='<h2>Email verification required</h2><p>Please open the verification email sent to your inbox and click the verification link before drafting and submitting a manuscript.</p>';}form.style.display='none';return;}if(notice){notice.style.display='block';notice.innerHTML='<p class="member-mini">Signed in as '+escapeHtml(member.email)+'. Manuscripts are saved to Cloudflare D1 and figures are stored in Cloudflare R2 when submitted.</p>';}for(let i=0;i<4;i++)addAuthorRow();document.getElementById('addAuthorRow')?.addEventListener('click',()=>addAuthorRow());document.getElementById('introductionSection').appendChild(makeEditor({id:'introduction',heading:'Introduction',allowFigure:true}));addResultsSection(false);document.getElementById('discussionSection').appendChild(makeEditor({id:'discussion',heading:'Discussion',allowFigure:true}));document.getElementById('methodsSection').appendChild(makeEditor({id:'methods',heading:'Materials and Methods',allowFigure:false}));document.getElementById('referencesSection').appendChild(makeEditor({id:'references',heading:'References',allowFigure:false}));document.getElementById('addResultsSection').addEventListener('click',addResultsSection);const draftId=new URLSearchParams(location.search).get('draft');let previous=null;if(draftId){previous=await findDraft(draftId);loadDraftIntoForm(previous);}form.addEventListener('submit',async e=>{e.preventDefault();if(!validateSubmission())return;document.querySelectorAll('.paper-section-editor').forEach(editor=>{if(editor.dataset.saved!=='true')saveSection(editor);});const submission=collectSubmission(previous);try{const saved=await upsertMemberPaper(submission,'save');if((window.AIBIO?.memberPaymentBypassEmail||'')===String(member.email||'').toLowerCase()){await upsertMemberPaper(saved,'formal-submit');location.href='/member/dashboard/?submitted=1';return;}location.href='/submit/payment/?id='+encodeURIComponent(saved.id);}catch(err){alert(err.message||'Could not save manuscript to D1/R2.');}});}
document.addEventListener('DOMContentLoaded',bindSubmit);
